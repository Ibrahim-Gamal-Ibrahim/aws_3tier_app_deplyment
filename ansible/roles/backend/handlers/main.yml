---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart backend
  ansible.builtin.systemd:
    name: todo-backend
    state: restarted
  become: true